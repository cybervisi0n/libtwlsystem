# libtwlsystem
Drop-in replacement for TwlSystem that targets Nintendo DS(i), PC, and other platforms.

libtwlsystem makes it easier to port Nintendo DS(i) games to PC and other platforms by handling the logic for common libraries used by nearly all Nintendo DS(i) games.
